function getMiddle(s) {
  const arr = s.split('');

  if(arr.length % 2 === 0) {
    
  }

    for (let i = 0; i < arr.length; i++) {
        if()
    }

  return console.log(arr.length);
}

getMiddle('test');
